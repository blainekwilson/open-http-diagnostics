# NGINX Level 2

Native configuration can forward an existing `traceparent`, but generating and validating a compliant value generally requires an implementation such as OpenResty or a native module. See [`implementations/nginx-openresty`](../../implementations/nginx-openresty/README.md).
